# Secrets — never commit
.env
.env.local
.env.*.local
*.pem
*.key

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml
profiles.yml

# Streamlit
.streamlit/secrets.toml

# Seed state (legacy; current state file IS committed for CI persistence)
seed/.seed_state.json
# Note: seed/.hubspot_ids.json is intentionally tracked — the weekly_seed.yml
# workflow commits it back after each Monday run so subsequent runs share state.

# Scratch JSON dumps from ad-hoc inspection scripts
inspect_*.json

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp

# Claude Code — local-only settings (project-wide settings.json may be committed)
.claude/settings.local.json
.claude/.credentials.json

# Internal project brief — local only, not part of public portfolio
CLAUDE.md
