# Pinned for reproducibility — bump deliberately, not accidentally.
# Grouped by phase; only what's currently in use is uncommented.

# ── Phase 1: HubSpot foundation ──────────────────────────────────────────────
python-dotenv==1.0.1
requests==2.32.3

# ── Phase 2: mock data generation ────────────────────────────────────────────
Faker==30.8.2

# ── Phase 4+: Snowflake connection (also used by extraction, reverse ETL, Streamlit)
snowflake-connector-python==3.12.3

# ── Phase 6+: dbt ────────────────────────────────────────────────────────────
dbt-core==1.8.7
dbt-snowflake==1.8.4

# ── Phase 12: Streamlit dashboard ────────────────────────────────────────────
streamlit==1.39.0
pandas==2.2.3
plotly==5.24.1

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==8.3.3
